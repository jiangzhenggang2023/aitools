package config

var (
	// 定义路由路径
	HelloPath  = "/hello"
	HealthPath = "/health"
	UploadPath = "/v1/document/upload"
	AuthPath   = "/v1/auth/login"
)

const (
	METASEARCH_SERVER_URL = "http://http://127.0.0.1:5001/v1/document/upload" // 替换为实际的服务器地址
)
